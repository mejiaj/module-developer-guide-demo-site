# Route definitions for the anytown module.

# Each route needs a unique identifier. We recommend prefixing the route
# name with the name of your module. Indented under the route name is the
# definition of the route. A routing.yml file can contain any number of routes.
anytown.weather_page:
  # The URL path where this page will be displayed.
  path: "/weather"
  defaults:
    # Title of the page used for things like <title> tag.
    _title: "Weather at the market"
    # Defines which method, on which class, should be called to get the
    # page content.
    _controller: '\Drupal\anytown\Controller\WeatherPage::build'
  requirements:
    # The permissions a user needs to have to see this page.
    _permission: "access content"
